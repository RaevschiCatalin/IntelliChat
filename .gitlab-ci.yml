<<<<<<< HEAD
# This file is a template, and might need editing before it works on your project.
docker-build-master:
  # Official docker image.
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build --pull -t "$CI_REGISTRY_IMAGE" .
    - docker push "$CI_REGISTRY_IMAGE"
  only:
    - master

docker-build:
  # Official docker image.
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build --pull -t "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG" .
    - docker push "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG"
  except:
    - master
deploy:
  image: docker:latest
  stage: deploy
  services:
    - docker:dind
  dependencies:
    - docker-build
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
    - docker pull "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG"
    - docker stop my-local-container || true
    - docker rm my-local-container || true
    - docker run -d --name my-local-container -p 80:80 "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG"
  only:
    - master
=======
image: gradle:8.5-jdk21

services:
  - docker:dind


stages:
  - build
  - test
  - deploy

# Optional: Check Gradle version before the script
#before_script:
#  - gradle --version

build:
  stage: build
  script:
    - gradle jib

test:
  stage: test
  script:
    - gradle test
    - echo "Tests completed successfully!"

deploy:
  stage: deploy
  image: docker:latest
  script:
    - echo "Deploying image..."
    - docker pull git-lab.ctif.md:4567/catalin.raebschi/backend:latest
    - docker run --rm git-lab.ctif.md:4567/catalin.raebschi/backend:latest
  only:
    - master
>>>>>>> backend/master
